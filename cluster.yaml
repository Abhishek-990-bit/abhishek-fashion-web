apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: abhishek-eks-cluster
  region: ap-south-1

managedNodeGroups:
  - name: standard-nodes
    instanceType: t3.small
    desiredCapacity: 2